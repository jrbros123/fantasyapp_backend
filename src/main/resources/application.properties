# JDBC URL (no ?sslmode in the string itself)
spring.datasource.url=jdbc:postgresql://db.uxpfegcqtjhosziafysh.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=UPPERCASE_lowercase

# Force SSL mode explicitly via Hikari
spring.datasource.hikari.data-source-properties.sslmode=require

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
